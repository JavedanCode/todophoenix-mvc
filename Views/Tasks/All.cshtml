@model IEnumerable<TodoPhoenix.Models.TaskItem>

<h2>All Tasks</h2>

<ul>
@foreach (var task in Model)
{
    <li>
        <strong>@task.Title</strong> - @task.Project?.Name
        (@(task.IsCompleted ? "Done" : "Pending"))
    </li>
}
</ul>